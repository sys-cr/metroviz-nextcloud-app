<!--
  Thanks for the PR! A short description in the box below + the
  checklist at the bottom keeps reviews fast.
-->

## What does this change?

<!-- One paragraph: what the user sees or what the system now does
     that it didn't before. -->

## Why?

<!-- The motivation. If it fixes an open issue, link it ("Closes
     #123") so GitHub auto-closes on merge. -->

## How was it tested?

<!-- Tests added/updated, manual verification steps, browser/OS combo. -->

---

## Checklist

- [ ] Tests pass locally
- [ ] Both `locales/de/translation.json` and `locales/en/translation.json` updated if user-visible strings changed
- [ ] No edits inside `js/metroviz/` (upstream fork — adapter modules instead) unless explicitly justified in the PR description
