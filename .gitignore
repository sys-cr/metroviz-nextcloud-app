# Dependencies
node_modules/

# Build / test artefacts
storybook-static/
coverage/
.vitest-cache/
vitest.config.js.timestamp-*.mjs

# Vite production build output — re-generated by `npm run build`.
# Production deployments ship `js/dist/` via the release tarball; the
# repo stays source-only.
js/dist/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
.claude/

# Local env files (never check in secrets)
.env
.env.local
.env.*.local

# Composer (PHP) — `/vendor` is generated by `composer install`.
# Leading slash anchors to the repo root so the rule does NOT match
# `js/vendor/` (which holds the vendored browser libs and IS checked in).
/vendor/

# Planning vault and internal methodology docs — local only.
# Defence in depth: the vault lives outside this repo by default
# (sibling directory), but if anyone accidentally places one of these
# inside the working tree, it must never be tracked.
**/vault/
**/Metro-Vault/
**/Blueprint-Vault/
**/*-Vault/
**/SECURITY-AUDIT.md
**/Implementierungs-Log.md
**/Bug-Tracking.md
**/Session-Log-*.md
**/*-Audit-Report.md
**/Threat-Model*.md
**/SOAK-TEST-PLAN.md
**/LOAD-TEST-PLAN.md
**/PERFORMANCE-PROFILE.md
**/KEYBOARD-NAV.md
**/SCREEN-READER-TEST-PLAN.md
**/Tech-Debt-Backlog.md
